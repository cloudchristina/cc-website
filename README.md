# cc-website

Personal website built with Next.js.

## Dev

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Structure

- `src/app` — pages and routes
- `src/components` — UI components
- `src/lib` — utilities
- `content/` — writing, notes, projects (markdown)
